# FastAPI and Server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.9.0
pydantic-settings>=2.5.0
annotated-doc>=0.0.2  # Required by fastapi
starlette>=0.40.0,<0.51.0  # Required by fastapi

# AI/ML Providers
google-generativeai>=0.8.0  # Gemini API
google-ai-generativelanguage==0.6.15  # Required by google-generativeai
google-api-python-client>=2.0.0  # Required by google-generativeai

# LiveKit (for real-time interviews)
livekit>=0.14.0
livekit-agents>=0.11.0
# livekit-plugins-hedra>=0.1.0  # Hedra plugin (check Hedra docs for latest version)
livekit-plugins-google>=0.1.0  # Google/Gemini LLM support
livekit-plugins-deepgram>=0.7.0
livekit-plugins-hedra>=1.3.0
livekit-plugins-turn-detector>=0.4.0  # EOUModel for better turn detection

# HTTP Requests
requests>=2.32.0
httpx>=0.27.0

# File Processing
PyPDF2>=3.0.1
python-docx>=1.1.2
lxml>=3.1.0  # Required by python-docx
pdfplumber>=0.11.0

# Audio Processing (if using local Whisper)
# NOTE: These are ONLY needed if you want to run Whisper locally (not via API)
# The current code uses API-based STT (OpenAI/Deepgram), so these can be skipped
# If you need local Whisper, install LLVM first: brew install llvm
# openai-whisper>=20231117  # Uncomment if using local Whisper
# torch>=2.2.0  # Uncomment if using local Whisper - Requires LLVM for llvmlite build

# Security
slowapi>=0.1.9  # Rate limiting
google-auth>=2.0.0  # Google OAuth token verification

# Utilities
python-dotenv>=1.0.1
python-dateutil>=2.9.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Optional: Database (for production)
# sqlalchemy==2.0.23
# psycopg2-binary==2.9.9  # PostgreSQL
# alembic==1.12.1  # Database migrations

